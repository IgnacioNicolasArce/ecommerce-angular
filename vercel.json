{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist/ecommerce-app",
  "routes": [
    { "src": "/assets/(.*)", "dest": "/assets/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}